[package]
name = "terminal"
version = "0.1.0"
edition = "2021"

[dependencies]
tinyos-app = { path = "../sdk" }
abi = { package = "tinyos-abi", path = "../../crates/abi" }
termcore = { path = "../../crates/termcore" }

[[bin]]
name = "terminal"
path = "src/main.rs"
