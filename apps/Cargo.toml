[workspace]
resolver = "2"
members = ["sdk", "hello", "pixels", "solitaire", "greet", "tui", "progress", "view", "vi", "clock", "top", "edit"]

[profile.release]
opt-level = 2
lto = true
panic = "abort"
